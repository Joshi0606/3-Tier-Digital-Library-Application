apiVersion: v1
kind: ServiceAccount
metadata:
  name: library-sa
  namespace: library
  annotations:
    # IRSA — links this ServiceAccount to the IAM role.
    # The workflow renders this at deploy time from the current AWS account.
    eks.amazonaws.com/role-arn: "arn:aws:iam::000000000000:role/digital-library-prod-app-pod-role"
